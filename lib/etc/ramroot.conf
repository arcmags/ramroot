# ramroot configuration file
##==========================  ramroot.conf  ==========================##

RAMROOT_PART_MOUNTS=()
RAMROOT_PART_UUIDS=()

# don't keep by default, but bind mounts to ramroot parts are kept:
LOCAL_PART_MOUNTS=()

PROMPT_DEFAULT=no
PROMPT_TIMEOUT=5

RAM_MEGS_MINIMUM=500
RAM_MEGS_PREFERRED=2000

ZRAM_MEGS_MINIMUM=500
ZRAM_MEGS_MAXIMUM=2000

USE_RAMROOT_ISSUE=yes
